package lib.models;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
{{#imports}}
{{#enums}}
import lib.enums.{{.}};
{{/enums}}
{{#models}}
import lib.models.{{.}};
{{/models}}
{{/imports}}

public class {{class_name}}{

  {{#properties}}
  {{property_type}}{{#isArray}}[]{{/isArray}} {{property_name}};
  {{/properties}}

  public {{class_name}}(JsonElement jsonElement){
    Gson gson = new Gson();
    this = gson.fromJson(jsonElement, {{class_name}}.class);
  }
  
  {{#properties}}
  public {{property_type}}{{#isArray}}[]{{/isArray}} get{{Property_Name}}(){
    return {{property_name}};
  }
  {{#isRequest}}
  public void set{{Property_Name}}({{property_type}}{{#isArray}}[]{{/isArray}} {{property_name}}){
    this.{{property_name}} = {{property_name}};
  }
  {{/isRequest}}
  {{/properties}}
}
